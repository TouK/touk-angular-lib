{
  "author": "touk",
  "name": "touk-angular-lib",
  "description": "validators, helpers",
  "version": "0.3.9",
  "homepage": "",
  "repository": {
    "type": "git",
    "url": ""
  },
  "scripts": {
    "build": "brunch build --production",
    "start": "brunch watch --server",
    "test": "karma start test/karma.conf.coffee",
    "test-single-run": "karma start test/karma.conf.coffee --single-run",
    "update-webdriver": "webdriver-manager update",
    "protractor": "protractor test/protractor.conf.coffee"
  },
  "dependencies": {
    "bower": ">= 1.0 < 1.4",
    "brunch": ">= 1.0 < 2.0",
    "coffee-script": ">= 1.7 < 1.8",
    "coffee-script-brunch": ">= 1.0 < 2.0",
    "javascript-brunch": ">= 1.0 < 2.0",
    "uglify-js-brunch": ">= 1.0 < 2.0"
  },
  "devDependencies": {
    "npm": ">= 3.5 < 3.6",
    "protractor": ">= 3.0 < 3.1",
    "karma": ">= 0.13 < 0.14",
    "karma-cli": ">= 0.1 < 0.2",
    "karma-jasmine": ">= 0.3 < 0.4",
    "karma-junit-reporter": "~0.2.2",
    "karma-html-reporter": "~0.2.3",
    "karma-coverage": "~0.2.6",
    "karma-spec-reporter": "0.0.13",
    "karma-chrome-launcher": ">= 0.2 < 0.3",
    "karma-phantomjs-launcher": "~0.1.4",
    "karma-coffee-preprocessor": ">= 0.3 < 0.4"
  }
}
